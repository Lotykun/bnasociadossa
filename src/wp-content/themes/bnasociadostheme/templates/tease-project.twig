{% extends "tease.twig" %}

{% block content %}
        <a href="{{post.link}}" data-post='{{post.post_title}}'>
            <img src="{{post.thumbnail.src}}" class="showUP"/>
        </a>
{% endblock %}